CREATE TABLE users (id INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
username VARCHAR(32) NOT NULL UNIQUE,
password VARCHAR(32) NOT NULL,
passwordhash blob(16) NOT NULL);

CREATE TABLE history (id INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
user_id INT UNSIGNED NOT NULL,
query VARCHAR(2048) NOT NULL);

CREATE INDEX user_id ON history (user_id);
